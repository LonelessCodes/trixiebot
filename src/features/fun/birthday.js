const TreeCommand = require("../../class/TreeCommand");
const BaseCommand = require("../../class/BaseCommand");

module.exports = function install(cr, client, config, database) {
    // const birthday = cr.registerCommand("birthday", new class extends TreeCommand {

    // });

    // birthday.registerSubCommand("remove", new class extends BaseCommand {

    // });
    // birthday.registerSubCommand("month", new class extends BaseCommand {

    // });

    // birthday.registerDefaultCommand(new class extends BaseCommand {

    // });
    // birthday.registerSubCommandAlias("*", "add");
};