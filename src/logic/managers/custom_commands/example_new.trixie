// current goal:

num = 0;
for (ch, i of $guild.channels) {
    num += 1;
}

ch = Channel($msg.mentions.channels[0]);
embed = RichEmbed().setTitle("Embed OWOWOWOWO");
msg, err = ch.send("owo", embed);
if (err != null) {
    reply;
}
